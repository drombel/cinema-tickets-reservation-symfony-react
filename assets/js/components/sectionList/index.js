import React from 'react';
// import './sectionList.scss';
import Item from './sectionListItem';

export default (props) => {

    return(
        <div className="section-list">
            <Item title={} text={} background={} link={}/>
        </div>
    );
}